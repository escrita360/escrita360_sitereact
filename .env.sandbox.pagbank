# ================================
# 🧪 SANDBOX PAGBANK - CONFIGURAÇÃO
# ================================

# Para testar o sandbox PagBank, configure as variáveis abaixo:
# 1. Obtenha suas credenciais em: https://dev.pagbank.uol.com.br/
# 2. Copie este arquivo para .env
# 3. Substitua os valores pelos seus tokens reais

# Ambiente (sandbox para testes, production para produção)
VITE_PAGBANK_ENV=sandbox

# ================================
# CREDENCIAIS PAGBANK SANDBOX
# ================================

# Token de acesso (obrigatório)
# Como obter: Painel do desenvolvedor → Sandbox → Tokens
VITE_PAGBANK_TOKEN=your_pagbank_sandbox_token_here

# ID da aplicação Connect (obtido ao criar aplicação)
VITE_PAGBANK_APP_ID=your_pagbank_app_id_here

# Credenciais OAuth do PagBank Connect (obrigatórias para Connect)
# Como obter: POST /oauth2/application
VITE_PAGBANK_CLIENT_ID=your_pagbank_client_id_here
VITE_PAGBANK_CLIENT_SECRET=your_pagbank_client_secret_here

# URL de redirecionamento após autorização do usuário
# Deve ser a mesma configurada ao criar a aplicação
VITE_PAGBANK_REDIRECT_URI=http://localhost:5173/connect/callback

# ================================
# URLs DE CALLBACK (PRÉ-CONFIGURADAS)
# ================================

# URL para receber notificações de pagamento
VITE_PAGBANK_WEBHOOK_URL=http://localhost:3000/webhooks/pagbank

# URLs de redirecionamento após pagamento
VITE_PAGBANK_REDIRECT_SUCCESS_URL=http://localhost:5173/pagamento-sucesso
VITE_PAGBANK_REDIRECT_CANCEL_URL=http://localhost:5173/pagamento-cancelado

# ================================
# OUTRAS CONFIGURAÇÕES (OPCIONAIS)
# ================================

# URL da API backend (se existir)
VITE_API_URL=http://localhost:3000

# Configurações Stripe (para comparação)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_key_here

# ================================
# INSTRUÇÕES DE USO
# ================================

# 1. Configure pelo menos VITE_PAGBANK_TOKEN
# 2. Execute: pnpm dev
# 3. Acesse: http://localhost:5173/sandbox/pagbank
# 4. Ou execute: pnpm run test:pagbank

# ================================
# LINKS ÚTEIS
# ================================

# 📚 Documentação: https://dev.pagbank.uol.com.br/
# 🎮 Painel Dev: https://pagseguro.uol.com.br/desenvolvedor/
# 🧪 Sandbox: http://localhost:5173/sandbox/pagbank
# 📖 README: ./README_SANDBOX.md
# ================================
# 🚀 PRODUÇÃO PAGBANK - CONFIGURAÇÃO
# ================================

# Este arquivo contém as configurações para o ambiente de PRODUÇÃO do PagBank.
# ⚠️ IMPORTANTE: Nunca commite este arquivo com credenciais reais no Git!

# Ambiente (production para ambiente real de produção)
VITE_PAGBANK_ENV=production

# ================================
# CREDENCIAIS PAGBANK PRODUÇÃO
# ================================

# Token de acesso PRODUÇÃO (obrigatório)
# Como obter: Painel do PagBank → Produção → Credenciais → Token
# Documentação: https://dev.pagbank.uol.com.br/
VITE_PAGBANK_TOKEN=your_pagbank_production_token_here

# ID da aplicação Connect (obtido ao criar aplicação)
VITE_PAGBANK_APP_ID=your_pagbank_production_app_id_here

# Credenciais OAuth do PagBank Connect (obrigatórias para Connect)
# Como obter: POST /oauth2/application no ambiente de produção
VITE_PAGBANK_CLIENT_ID=your_pagbank_production_client_id_here
VITE_PAGBANK_CLIENT_SECRET=your_pagbank_production_client_secret_here

# URL de redirecionamento após autorização do usuário
# Deve ser HTTPS e a mesma configurada ao criar a aplicação
VITE_PAGBANK_REDIRECT_URI=https://yourdomain.com/connect/callback

# ================================
# URLs DE CALLBACK (PRODUÇÃO)
# ================================

# URL para receber notificações de pagamento (webhooks)
# Deve ser uma URL pública e HTTPS
# Exemplo: https://suaempresa.com.br/webhooks/pagbank
VITE_PAGBANK_WEBHOOK_URL=https://yourdomain.com/webhooks/pagbank

# URLs de redirecionamento após pagamento
VITE_PAGBANK_REDIRECT_SUCCESS_URL=https://yourdomain.com/pagamento/resultado?status=success
VITE_PAGBANK_REDIRECT_CANCEL_URL=https://yourdomain.com/pagamento/resultado?status=cancel

# ================================
# OUTRAS CONFIGURAÇÕES
# ================================

# URL da API backend (produção)
# Deve ser uma URL pública e HTTPS
VITE_API_URL=https://yourdomain.com/api

# ================================
# INSTRUÇÕES DE USO
# ================================
# 1. Copie este arquivo para .env (ou .env.production)
# 2. Substitua todas as variáveis 'your_*' pelos valores reais de produção
# 3. Configure as URLs com seu domínio real
# 4. Configure o webhook no painel do PagBank para receber notificações
# 5. Certifique-se de que todas as URLs são HTTPS (não HTTP)
# 6. Teste em sandbox antes de ativar em produção
# 7. Adicione .env ao .gitignore para não commitar credenciais

# ================================
# SEGURANÇA
# ================================
# ⚠️ NUNCA compartilhe ou commite suas credenciais de produção
# ⚠️ Use HTTPS em todas as URLs de produção
# ⚠️ Configure certificado SSL válido em seu domínio
# ⚠️ Monitore logs de webhook para detectar anomalias
# ⚠️ Mantenha o token seguro e renove-o periodicamente

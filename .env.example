# Backend API URL
VITE_API_URL=http://localhost:5000/api

# Configuracoes PagBank (apenas para pagina de testes/sandbox - opcional)
# NOTA: Tokens e credenciais sensiveis devem estar APENAS no backend (server/.env)
# As chamadas de producao sao feitas exclusivamente pelo backend
VITE_PAGBANK_ENV=sandbox

# Stripe Publishable Key (opcional, se usar Stripe no frontend)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here
